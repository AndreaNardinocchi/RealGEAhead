/**
 * Payment utility.
 * Scaffolding creating a payment, and we will integrate Stripe as the actual payment processor.
 */
export async function createPayment(amount, bookingId) {
  // ToDo Implementing payment creation
  console.log("Payment: ", { amount, bookingId });

  // Example return:
  // return { paymentId: "pay_123", status: "created" };
}
